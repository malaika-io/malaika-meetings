extends ./layout.pug
include ./components/mixin-admin

block content
    - var socket                = true;
    .chitchat-container.sidebar-toggle
        +sidebar
        +content
        +right-sidebar
        +app-sidebar
        +modal
